Address Decode Logic

Base address = 0x0000_0000

16 registers, each 32-bit = 64 bytes total

Address index = (ADDR - BASE) >> 2

Valid if:

Address aligned (ADDR[1:0] == 2'b00)

Address in range (less than 0x40)